services:
 postgres:
   image: postgres:17
   container_name: grocery-postgres
   restart: unless-stopped
   environment:
     POSTGRES_DB: grocerydb
     POSTGRES_USER: postgres
     POSTGRES_PASSWORD: postgres
   ports: 
    - "5432:5432"
   volumes: 
    - postgres-data:/var/lib/postgresql/data
   healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d grocerydb" ]
      interval: 5s
      timeout: 5s
      retries: 5
       
 api:
  build:
    context: ./GroceryStore
    dockerfile: Dockerfile
  container_name: grocery-api
  depends_on:
     postgres:
       condition: service_healthy
  environment:
    - ASPNETCORE_ENVIRONMENT=Development
    - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=grocerydb;Username=postgres;Password=postgres
    - JwtSettings__SecretKey=${JWT_TOKEN_KEY}
    - JwtSettings__Issuer=${JWT_ISSUER}
    - JwtSettings__Audience=${JWT_AUDIENCE}
    - JwtSettings__ExpirationHours=${JWT_EXPIRATION_HOURS}
  ports:
    - "8080:8080"

volumes:
  postgres-data:

